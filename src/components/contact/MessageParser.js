class MessageParser {
    parse(message) {
      // Implement your logic to handle different user messages
      // For now, let's assume the user's first message is their name
      return message;
    }
  }
  
  export default MessageParser;
  