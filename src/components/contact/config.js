// config.js
const config = {
    initialMessages: [
      { message: 'Hi there! What is your name?' },
    ],
    botName: 'SpacesBot',
    customStyles: {
      // Customize the chatbot UI styles if needed
    },
  };
  
  export default config;
  